# 轮1：检查成交量数据源代码

你在 `~/.etf-skill/scripts/` 工作。请只完成本轮任务，不要提前做后续轮次。

## 目标
检查 `etf_v7_threefactor.py` 的数据源代码，找出成交量数据获取逻辑，搞清楚：
1. 成交量数据从哪里来（API/字段/函数）。
2. 成交量如何存储/传递。
3. 量能分位数/量能因子如何计算。
4. 4只沪深300ETF相关代码列表在哪里定义。

## 输出要求
- 新建或更新 `RALPH_ROUND1_NOTES.md`，记录上述发现，包含函数名/行号/关键公式。
- 不要修改主逻辑代码。
- 最后输出 `COMPLETE`。
